import { Config } from "./config.js";

export class Enemies{
    config: Config;
    constructor(config: Config) {
        this.config = config;
    }

    draw() {
        
    }
}

export class Enemy {
    config: Config;
    constructor(config: Config) {
        this.config = config;
    }

    draw() {
        
    }
}
